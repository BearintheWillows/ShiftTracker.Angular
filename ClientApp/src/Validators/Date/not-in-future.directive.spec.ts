import { NotInFutureValidatorDirective } from './not-in-future-validator.directive';

describe('NotInFutureValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NotInFutureValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
