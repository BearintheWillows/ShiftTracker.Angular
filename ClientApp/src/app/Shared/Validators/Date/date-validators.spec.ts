import { DateValidatorsDirective } from './date-validators.directive';

describe('DateValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new DateValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
