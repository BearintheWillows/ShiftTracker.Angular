import { TimeFormatForUiPipe } from './time-format-for-ui.pipe';

describe('TimeFormatForUiPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormatForUiPipe();
    expect(pipe).toBeTruthy();
  });
});
