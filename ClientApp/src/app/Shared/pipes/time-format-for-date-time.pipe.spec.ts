import { TimeFormatForDateTimePipe } from './time-format-for-date-time.pipe';

describe('TimeFormatForDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormatForDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
