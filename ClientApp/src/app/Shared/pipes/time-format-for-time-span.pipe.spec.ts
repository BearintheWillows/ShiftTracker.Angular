import { TimeFormatForTimeSpanPipe } from './time-format-for-time-span.pipe';

describe('TimeFormatForTimeSpanPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormatForTimeSpanPipe();
    expect(pipe).toBeTruthy();
  });
});
